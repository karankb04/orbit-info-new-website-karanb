# Google Apps Script Web App endpoint that appends quote-form leads to a Sheet.
# Public by nature (it ships in the client bundle) — this is for configurability,
# not secrecy. Falls back to the hardcoded default in LeadFormHandler.tsx if unset.
NEXT_PUBLIC_LEAD_FORM_ENDPOINT=
